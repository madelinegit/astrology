Previously deployed application taken offline due to size of database, cost.
To run this application....

IN TERMINAL: 

pipenv shell, 
pipenv install flask pymysql, 
pipenv install flask-bcrypt, 
pipenv install cryptography,
python3 server.py 
